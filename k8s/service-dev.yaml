apiVersion: v1
kind: Service
metadata:
  name: task-api-service
  namespace: task-api-dev
  labels:
    app: task-api
spec:
  type: NodePort  # Accessible via node IP + port
  selector:
    app: task-api
  ports:
  - port: 80          # Service port
    targetPort: 5000  # Container port
    nodePort: 30080   # External access port (30000-32767 range)
    protocol: TCP
    name: http